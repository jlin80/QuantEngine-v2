# Runtime — Quant Engine V2 (Python 3.12)
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
httpx>=0.27,<1.0
websockets>=13.0,<16.0
# El techo <6.0 es DELIBERADO: `qevps` corre redis-py 5.3.1 y es la version con
# la que el motor esta validado en produccion. Subirlo cambiaria el cliente de
# Redis en el proximo despliegue sin que nadie lo haya probado alli.
# Nota: algun entorno de desarrollo tiene 8.0.0 instalado — es el ENTORNO el que
# se desvio del pin, no al reves. Ver `docs/architecture.md` (deriva de entorno).
redis>=5.0,<6.0
SQLAlchemy[asyncio]>=2.0.35,<3.0
asyncpg>=0.29,<1.0
alembic>=1.13,<2.0
psutil>=6.0,<7.0
