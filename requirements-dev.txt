# Desarrollo / QA — Quant Engine V2
-r requirements.txt
ruff>=0.6
# Pineado exacto: black cambia el formato entre versiones y un rango abierto
# hace que `black --check` falle al reinstalar el venv. Subir a mano y reformatear.
black==26.5.1
mypy>=1.11
pytest>=8.3
pytest-asyncio>=0.24
pytest-cov>=5.0
